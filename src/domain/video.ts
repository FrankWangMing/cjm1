interface monitorBaseInfo {
  id: number;
  path: string;
  title: string;
}

interface MonitorAllInfo {
  cameraId: number;
  code: string;
  districtName: string;
  latitude: number;
  longitude: number;
  baseAltitude: number;
}
